fastapi
uvicorn[standard]
pydantic
pydantic-settings
openai
httpx
mss
psutil
Pillow
python-multipart
websockets

# Desktop launcher (run_app.py) — native app window via OS WebView (EdgeWebView2 on Windows 11).
# Frameless: the web UI draws its own titlebar; pystray provides the minimize-to-tray icon.
pywebview
pystray

# Passive game-state OCR: GPU-based capture via the Windows Graphics Capture API (avoids the
# desktop-compositor stalls that GDI/BitBlt capture - what "mss" uses - causes in games) and
# text recognition via the built-in Windows OCR engine (Windows.Media.Ocr) - no external OCR
# binary to install separately, unlike the old Tesseract-based approach.
windows-capture
winrt-runtime
winrt-Windows.Media.Ocr
winrt-Windows.Graphics.Imaging
winrt-Windows.Storage.Streams
winrt-Windows.Foundation

# Optional: required only for Chirp 3 HD Voices (Google Cloud TTS).
# google-auth[requests]

# Per-application volume control (Windows Core Audio session API) - lets the companion turn a
# specific app/game up or down in the Windows volume mixer, separate from its own narration volume.
pycaw

# Extracts main article text from a fetched web page (strips nav/ads/boilerplate) so web_search's
# SearXNG path can hand the model real page content instead of a one-line search-engine snippet.
trafilatura

# Resolves a YouTube search query to the actual best-match video (no API key/quota) for the
# play_on_youtube tool.
yt-dlp

# YouTube Music search (unofficial, no login/API key needed for anonymous search) - lets
# play_on_youtube prefer the official-audio "song" upload over the official music video.
ytmusicapi


# Dev only: unit tests (run with `python -m pytest tests`).
pytest
