# Batch files must keep CRLF line endings or cmd.exe mis-parses them
# (eats the first character of each line). Force it regardless of core.autocrlf.
*.cmd text eol=crlf
*.bat text eol=crlf

# The shipped prebuilt binary — never touch its bytes.
*.exe binary
