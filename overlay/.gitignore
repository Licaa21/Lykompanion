# Build artifacts. overlay.exe itself IS shipped (prebuilt, Sprint C5) so users
# without VS Build Tools still get the overlay — it is force-tracked below.
*.obj
*.exe
*.pdb
*.ilk

!Lykompanion-overlay.exe
